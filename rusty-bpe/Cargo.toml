[package]
name = "rusty-bpe"
version = "0.1.0"
edition = "2024"

[dependencies]
hashbrown = "0.17.1"
rayon = "1.12.0"
regex = "1.12.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
